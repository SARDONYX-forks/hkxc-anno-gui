[workspace.package]
version = "0.0.0"
description = ""
categories = []
authors = ["beefclot"]
license = "GPL-3.0"
repository = "https://github.com/beefclot/hkxc-anno-gui"
readme = "README.md"
edition = "2021"
rust-version = "1.91"

[workspace]
members = [
  "crates/cli",
  "crates/gui/src-tauri",
  "crates/serde_hkx_hkanno",
  "crates/tracing_rotation",
]
resolver = "2"

[workspace.dependencies]
rayon = "1.11.0"
serde = { version = "1.0.228", features = ["derive"] } # Implement (De)Serialize
serde_json = "1.0.149"                                 # gui: To avoid generate_context error.
snafu = "0.8.9"
tokio = "1.49.0"
tracing = { version = "0.1.44" }                       # logger

# serde-hkx
havok_classes = { git = "https://github.com/SARDONYX-sard/serde-hkx", rev = "f49aa22", default-features = false, features = [
  "ignore_duplicates",
] }
havok_types = { git = "https://github.com/SARDONYX-sard/serde-hkx", rev = "f49aa22", default-features = false }
serde_hkx_features = { git = "https://github.com/SARDONYX-sard/serde-hkx", rev = "f49aa22", default-features = false }


# workspace members
serde_hkx_hkanno = { path = "crates/serde_hkx_hkanno" }
tracing_rotation = { path = "crates/tracing_rotation" }


# NOTE: Set this to lto because there is a part of CI that assumes release dir, and use lto only when the tag is typed.
[profile.release]
# NOTE: Optimizing to the limit reduces the size of the binary, but for some reason,
# the second async fn cannot be executed, resulting in an error, so it is turned off.
# codegen-units = 1
debug = false
# lto = true
opt-level = 3
# panic = 'abort'
strip = true

# Use release mode during development to prevent `serde-hkx` stack overflow
# - ref: https://doc.rust-lang.org/cargo/reference/profiles.html#release
[profile.release-no-lto]
inherits = "dev"
opt-level = 3
debug = false
strip = "none"
debug-assertions = false
overflow-checks = false
lto = false
panic = 'unwind'
incremental = false
codegen-units = 16
rpath = false
